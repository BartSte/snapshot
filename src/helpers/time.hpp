#include <chrono>
#include <cstdint>
#include <string>

std::string parseUnit(std::string str);
std::string parseNumber(std::string str);
std::chrono::milliseconds stringToMilliseconds(std::string str);
